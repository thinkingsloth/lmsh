# lmsh configuration file example
# Copy to ~/.config/lmsh/config and edit with your values

# =============================================================================
# OpenAI
# =============================================================================
base_url=https://api.openai.com/v1
api_token=sk-your-openai-api-key
model_id=chatgpt-4o-latest

# =============================================================================
# Claude (Anthropic)
# =============================================================================
# base_url=https://api.anthropic.com/v1
# api_token=sk-ant-your-anthropic-key
# model_id=claude-sonnet-4.5-20250514

# =============================================================================
# Custom OpenAI-compatible API
# =============================================================================
# base_url=http://127.0.0.1:7980/v1
# api_token=your-api-token
# model_id=your-model-name

# =============================================================================
# Optional Settings
# =============================================================================
# Default output format (bash, python, node, etc.)
# Defaults to current shell if not set
# output=bash

# Allow sudo commands (true/false)
# allow_sudo=false
